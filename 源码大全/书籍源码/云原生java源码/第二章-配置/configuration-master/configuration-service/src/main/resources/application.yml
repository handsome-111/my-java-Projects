server:
  port:
    8888
spring:
  cloud:
    config:
      server:
        git:
          uri:
            #https://github.com/cloud-native-java/config-server-configuration-repository
            https://github.com/handsome-111/my-java-Projects
          search-paths:
            -  testFiles/config-server-configuration-repository/
            
            
            